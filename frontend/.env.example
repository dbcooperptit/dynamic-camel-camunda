# Frontend configuration for dynamic route management
# Copy to .env.local (recommended) or .env and adjust.

# Optional tenant scoping header
VITE_TENANT_ID=default

# Optional API key header for /api/camel-routes endpoints (only needed if backend enables it)
VITE_API_KEY=
